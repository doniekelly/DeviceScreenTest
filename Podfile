source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '8.0'
inhibit_all_warnings!

workspace 'ScreenCap'

project 'Screen Capture.xcodeproj'

def app_pods
    pod 'ios-qr-encoder'
end

target "Screen Capture" do
    app_pods
end

target "Screen Capture client" do
    app_pods
end
